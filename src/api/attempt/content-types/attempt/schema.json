{
  "kind": "collectionType",
  "collectionName": "attempts",
  "info": {
    "singularName": "attempt",
    "pluralName": "attempts",
    "displayName": "attempt"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "aid": {
      "type": "uid",
      "required": true
    },
    "attempt": {
      "type": "json",
      "required": true
    },
    "question": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::question.question",
      "inversedBy": "attempts"
    }
  }
}
